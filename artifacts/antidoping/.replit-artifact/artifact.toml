kind = "web"
previewPath = "/"
title = "Anti-Doping for Athletes"
version = "1.0.0"
id = "artifacts/antidoping"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 8080

[services.development]
run = "pnpm --filter @workspace/antidoping run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/antidoping", "run", "build" ]
publicDir = "artifacts/antidoping/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "8080"
BASE_PATH = "/"
